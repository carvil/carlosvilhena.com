---
import BaseLayout from '../layouts/BaseLayout.astro';
import PostList from '../components/PostList.astro';
import { getCollection } from 'astro:content';
import { CATEGORIES } from '../content.config.ts';

const all = await getCollection('posts', ({ data }) => !data.draft);
const sorted = all.sort(
  (a, b) => b.data.publishedAt.valueOf() - a.data.publishedAt.valueOf(),
);

const byCategory = (Object.keys(CATEGORIES) as (keyof typeof CATEGORIES)[]).map(
  (cat) => ({
    slug: cat,
    label: CATEGORIES[cat].label,
    intro: CATEGORIES[cat].intro,
    posts: sorted
      .filter((p) => p.data.category === cat)
      .map((p) => ({
        id: p.id,
        url: `/${p.id.replace(/\/index$/, '').split('/').pop()}/`,
        frontmatter: p.data,
      })),
  }),
);
---

<BaseLayout title="Carlos Vilhena">
  <div class="max-w-3xl mx-auto">
    <section class="py-[8vh]" transition:animate="fade">
      <h1 class="text-5xl md:text-6xl mb-6 font-semibold">Carlos Vilhena</h1>
      <p class="text-xl md:text-2xl text-gray-500 dark:text-gray-400">
        Thoughts on entrepreneurship, leadership and engineering.
      </p>
    </section>

    {byCategory.map((cat) => (
      <section class="mt-12" transition:animate="fade">
        <header class="mb-2">
          <h2 class="text-2xl font-semibold">
            <a href={`/${cat.slug}/`} class="hover:underline">{cat.label}</a>
          </h2>
          <p class="text-gray-500 dark:text-gray-400 mt-1">{cat.intro}</p>
        </header>
        <PostList posts={cat.posts} showDescription={false} />
      </section>
    ))}
  </div>
</BaseLayout>
